// @flow
import { defineMessages } from 'react-intl';

const messages = defineMessages({
  noSpam: {
    id: 'Subscription.noSpam',
    defaultMessage:
      'We promise to never spam you or share your personal information!',
  },
});

export default messages;
