<!DOCTYPE html>
<html lang="<%= lang %>">
<head>
    <%# TODO: add GTM tag in production %>
    <title>Holo Apollo Art</title>
    <meta charset="utf-8">
    <meta name="viewport" content="minimum-scale=1, initial-scale=1, width=device-width, shrink-to-fit=no">
    <%- helmetData.meta.toString() %>
    <meta name="keywords" content="holo apollo, holoapollo, холо аполло, дизайнерские вещи, декор, купить дизайнерские вещи Украина">

    <link rel="shortcut icon" type="image/x-icon" href="<%= staticRoot %>/img/favicon.png" />
    <link href="https://fonts.googleapis.com/css?family=Montserrat:300i,400,500,600,700" rel="stylesheet" />
    <style id="jss-server-side"><%- css%></style>
    <style>
        body {
            font-family: 'Montserrat', Arial, sans-serif;
            -webkit-text-size-adjust: none;
            color: #424242;
        }

        button {
            font-family: inherit;
        }

        h1, h2, h3, h4, h5, h6 {
            margin: 0;
        }
    </style>
    <%- styleTags %>
</head>

<body>
<div id="app"><%- reactDom %></div>

<% bundlePaths.forEach(function(bundle) { %>
    <script src="<%= staticRoot %><%= bundle %>"></script>
<% }); %>

</body>
</html>
